const name = "snigdha" //decl of a string
const repoCount = 22

console.log(name + repoCount + "Value"); // dont use this syntax

console.log(`hello my name is ${name} and my repo count is ${repoCount}`);// use backtiks instead pf the above syntax , use for string interpolletoin

const gameName = new String("snigdha") // new is a keywoed it used to use obj  in js , obj invoke 
 console.log(gameName[0]); 
 console.log(gameName.__proto__);
 console.log(gameName.length)